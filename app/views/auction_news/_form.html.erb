<%= form_with(model: auction_news, local: true) do |form| %>
  <% if auction_news.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(auction_news.errors.count, "error") %> prohibited this auction_news from being saved:</h2>

      <ul>
      <% auction_news.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
